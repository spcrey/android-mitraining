<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/area_search"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:textSize="24sp"
        android:gravity="center"
        android:background="@drawable/rounded_rectangle"
        android:layout_marginTop="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/text_xiaomi_count_help_center"
        />

    <TextView
        android:id="@+id/icon_search"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@color/teal_200"
        android:layout_marginStart="12dp"
        app:layout_constraintLeft_toLeftOf="@+id/area_search"
        app:layout_constraintTop_toTopOf="@+id/area_search"
        app:layout_constraintBottom_toBottomOf="@+id/area_search"
        />

    <TextView
        android:id="@+id/text_search"
        android:layout_width="240dp"
        android:layout_height="0dp"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:gravity="center_vertical"
        android:textSize="18sp"
        android:text="请输入问题关键词"
        app:layout_constraintStart_toEndOf="@+id/icon_search"
        app:layout_constraintTop_toTopOf="@+id/area_search"
        app:layout_constraintBottom_toBottomOf="@+id/area_search"
        />

</merge>